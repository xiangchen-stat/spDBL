% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/func_FF.R
\name{read_big_csv_quick}
\alias{read_big_csv_quick}
\title{Read a rectangular block from a large CSV file}
\usage{
read_big_csv_quick(filename, rows = c(1, Inf), cols = c(1, Inf), header = TRUE)
}
\arguments{
\item{filename}{Character. Path to the CSV file.}

\item{rows}{Integer vector of length 2 giving the first and last row indices
to read (1-based, excluding the header row if \code{header = TRUE}).
Use \code{c(1, Inf)} to read all rows. Defaults to \code{c(1, Inf)}.}

\item{cols}{Integer vector of length 2 giving the first and last column
indices to read. Use \code{c(1, Inf)} to read all columns. Defaults to
\code{c(1, Inf)}.}

\item{header}{Logical. Whether the file has a header row. Defaults to
\code{TRUE}.}
}
\value{
A \code{tibble} (from \code{readr}) containing the requested block.
}
\description{
Efficiently reads a contiguous rectangular block of rows and columns from a
CSV file using \code{readr::read_csv}. Useful for processing big data in
chunks without loading the entire file into memory.
}
